case PA_DEFENDER:
    // Other skill effects...
    if (sd) {
        // No movement speed modification
        // pc_set_walkspeed(sd, sd->status.walkspd - 50); // Removed speed reduction
    }
    break;
